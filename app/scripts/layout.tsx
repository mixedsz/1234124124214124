import type { Metadata } from 'next';
export const metadata: Metadata = { title: 'FiveM Scripts – QBCore, Qbox & ESX' };
export default function Layout({ children }: { children: React.ReactNode }) { return <>{children}</>; }
